<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.ahuja.dietanalyzer">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <receiver
            android:name=".Adapters_Fragments.NotificationReceiver"
            android:enabled="true" />

        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".TodaySummaryActivity" />
        <activity android:name=".AddMeal" />
        <activity android:name=".SearchItem" />
        <activity android:name=".DetailTest" />
        <activity android:name=".AddExercise" />
        <activity android:name=".AddWeight" />
        <activity android:name=".AddMealOption" />
        <activity android:name=".AddMealManual" />
        <activity android:name=".MyFoodAddSearch" />
        <activity android:name=".AddWater" />
        <activity android:name=".WeekSummary" />
        <activity android:name=".MyProfile" />
        <activity
            android:name=".ProgressSoFar"
            android:label="@string/title_activity_progress_so_far"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".ProgressActivity"></activity>
    </application>

</manifest>